import {
  __commonJS
} from "./chunk-OL3AADLO.js";

// node_modules/scale-svg-path/index.js
var require_scale_svg_path = __commonJS({
  "node_modules/scale-svg-path/index.js"(exports, module) {
    module.exports = function(segments, sx, sy) {
      sy = !sy && sy !== 0 ? sx : sy;
      return segments.map(function(segment) {
        var name = segment[0].toLowerCase();
        if (name === "v") {
          segment[1] *= sy;
          return segment;
        }
        if (name === "a") {
          segment[1] *= sx;
          segment[2] *= sy;
          segment[6] *= sx;
          segment[7] *= sy;
          return segment;
        }
        return segment.map(function(val, i) {
          if (!i) {
            return val;
          }
          return val *= i % 2 ? sx : sy;
        });
      });
    };
  }
});
export default require_scale_svg_path();
//# sourceMappingURL=scale-svg-path.js.map
